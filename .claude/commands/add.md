Add this to phalanX: $ARGUMENTS

Before writing any code:
1. Find where in app.js this belongs using the Section Map
2. Check what already exists that can be reused
3. Read index.html if UI is involved

Then implement. Stay in the relevant section — don't touch unrelated code.
