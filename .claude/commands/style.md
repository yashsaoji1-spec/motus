Make this UI change in phalanX: $ARGUMENTS

Before touching anything:
1. Find the relevant screen ID from the Screen System table in CLAUDE.md
2. Read the current HTML in index.html and CSS in styles.css for that screen
3. Check the CSS variables in .claude/rules/ui-rules.md — use existing tokens, don't hardcode colors

No emojis. No new CSS variables unless absolutely necessary.
