node_modules/
dist/
.vscode/
.DS_Store

# Environment files
.env.*
!.env.audit

# Service-account keys — full-admin credentials, never commit
serviceAccountKey.json
*-service-account*.json

# Firebase config (must stay in root for CLI, but not needed on GitHub)
.firebaserc
firebase.json
firestore.indexes.json

# Dev artifacts (Claude, plans, probes)
CLAUDE.md
CLAUDE.local.md
.claude/
TODO.md
ENVIRONMENT_PROBE.md
.octogent
.superpowers/
docs/*
!docs/superpowers/

# Native builds (Capacitor -- not deployed)
android/
ios/
capacitor.config.json

# Cloud Functions — track source, ignore installed deps
functions/node_modules/

# Tests — rules tests are tracked and passing; e2e (playwright) not set up yet
tests/e2e/
# Tests — committed: seed.mjs, audit harness; excluded: shot output and baseline artifacts
tests/audit/shots/
tests/audit/*-baseline.txt
playwright.config.js
vitest.config.js

# CI/CD workflows (not configured)
.github/

# ML angle tracking (feature disabled)
angle_tracking/

# Build/debug artifacts
.firebase/
firestore-debug.log
report.html
report.json
test-results/
playwright-report/
logs/

# Misc
!scripts/
scripts/*
!scripts/*.mjs
public/404.html
.npmrc
